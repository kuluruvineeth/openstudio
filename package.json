{
  "name": "turborepo-shadcn-ui",
  "version": "1.3.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "clean": "git clean -xdf node_modules",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:add": "pnpm --filter @openstudio/ui ui:add"
  },
  "devDependencies": {
    "@openstudio/eslint-config": "workspace:*",
    "@openstudio/typescript-config": "workspace:*",
    "prettier": "^3.3.2",
    "turbo": "2.0.6"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
